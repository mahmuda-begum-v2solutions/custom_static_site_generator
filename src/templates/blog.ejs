<h2><%= title %></h2>
<p><em>Posted on <%= date %> by <%= author %></em></p>

<article>
  <%- content %>
  <!-- Markdown content goes here -->
</article>

<% if (tags?.length) { %>
<p>Tags: <%= tags.join(", ") %></p>
<% } %>
