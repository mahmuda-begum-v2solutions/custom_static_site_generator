<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <meta name="description" content="<%= description || '' %>" />
    <meta name="author" content="<%= author || '' %>" />
  </head>
  <body>
    <header>
      <h1><%= title %></h1>
      <% if (date) { %>
      <p><em><%= date %></em></p>
      <% } %>
    </header>

    <main>
      <!-- Inject inner page template or markdown fallback -->
      <%- content %>
    </main>

    <footer>
      <p>Author: <%= author %></p>
      <% if (tags?.length) { %>
      <p>Tags: <%= tags.join(", ") %></p>
      <% } %>
    </footer>
  </body>
</html>
